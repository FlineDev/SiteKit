# Reference

Every configuration option in one place.

@Metadata {
   @TitleHeading("Reference")
   @PageKind(article)
}

## Options

Document each option here. Tables render from standard Markdown:

| Option | Default | Description |
|---|---|---|
| `name` | – | The site name shown in the header. |
| `baseURL` | – | The canonical origin for absolute URLs. |
